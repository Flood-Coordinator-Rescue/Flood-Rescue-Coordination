{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "watchPatterns": ["./backend/**/*"],
    "dockerfilePath": "./backend/Dockerfile"
  },
  "deploy": {
    "healthcheckPath": "/api/v1/swagger-ui.html",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}